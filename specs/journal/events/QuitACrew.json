{
	"description": "Quit a multicrew session",
	"properties": {
		"timestamp": {
			"type": "string",
			"format": "date-time"
		},
		"event": {
			"type": "string",
			"enum": ["QuitACrew"]
		},
		"Captain": {
			"type": "string"
		}
	},
	"type": "object",
	"required": ["timestamp", "event"],
	"title": "QuitACrew",
	"$schema": "http://json-schema.org/draft-07/schema#"
}
