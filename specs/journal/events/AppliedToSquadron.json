{
	"description": "Applied to join a squadron",
	"properties": {
		"timestamp": {
			"type": "string",
			"format": "date-time"
		},
		"SquadronName": {
			"type": "string"
		},
		"event": {
			"type": "string",
			"enum": ["AppliedToSquadron"]
		}
	},
	"type": "object",
	"required": ["timestamp", "event"],
	"title": "AppliedToSquadron",
	"$schema": "http://json-schema.org/draft-07/schema#"
}
