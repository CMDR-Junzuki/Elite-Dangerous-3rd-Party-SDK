{
	"description": "Game shutdown",
	"properties": {
		"timestamp": {
			"type": "string",
			"format": "date-time"
		},
		"event": {
			"type": "string",
			"enum": ["Shutdown"]
		}
	},
	"type": "object",
	"required": ["timestamp", "event"],
	"title": "Shutdown",
	"$schema": "http://json-schema.org/draft-07/schema#"
}
